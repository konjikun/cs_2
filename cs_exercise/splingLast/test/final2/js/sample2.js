function runs(){
    if 
}

function run() {
    let x_elem = document.getElementById('x-input');
    let x = parseInt(x_elem.textContent);
    
    let result_elem = document.getElementById('result');
    result_elem.value = cloud_to_weather(x);
}